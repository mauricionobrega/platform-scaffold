// import * as constants from './constants'
import * as utils from '../../utils/utils'

export const shrinkHeader = utils.createAction('Shrunk the header')
export const expandHeader = utils.createAction('Expanded the header')
