export const init = () => {
    // initialize GA script
}

export const analyticReceiver = (type, metaPayload, state) => {
    switch (type) {
        case 'Pageview':
            // GA implementation
            break
    }
}
