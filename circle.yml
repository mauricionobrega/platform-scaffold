machine:
    node:
        version: 6.9.2
dependencies:
    cache_directories:
        - "web/node_modules"
    pre:
        - ./scripts/install-google-chrome-stable.sh
        - npm install:
            pwd: web
test:
    override:
        - npm run lint:
            pwd: web
        - npm test -- --runInBand:
            pwd: web
        - sudo -E npm run test:pwa-ci:
            pwd: web
        ## Enable smoke-test when automated workflow tests have been implemented.
        # - npm run smoke-test:
        #    parallel: true
        #    pwd: web
    post:
       - cp -R lighthouse/* $CIRCLE_ARTIFACTS:
           pwd: web
experimental:
    notify:
        branches:
            only:
                - master
                - develop
