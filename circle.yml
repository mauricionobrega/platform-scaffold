machine:
    node:
        version: 4.4.7
test:
    override:
        - npm run lint:circleci
        - npm test
experimental:
    notify:
        branches:
            only:
                - master
                - develop