machine:
    node:
        version: 4.4.7
test:
    override:
        - npm run lint
        - npm test -- --runInBand
        # Enable smoke-test when automated workflow tests have been implemented.
        # - npm run smoke-test
experimental:
    notify:
        branches:
            only:
                - master
                - develop
