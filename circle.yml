machine:
    node:
        version: 4.4.7
test:
    override:
        - npm run lint
        - npm test -- --runInBand
experimental:
    notify:
        branches:
            only:
                - master
                - develop
